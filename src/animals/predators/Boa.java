package animals.predators;

public class Boa extends Predator {
    public Boa(int weight, int maxAnimalInOnePlace, int maxSpeed, int fullySatisfiedFood) {
        super(weight, maxAnimalInOnePlace, maxSpeed, fullySatisfiedFood);
    }
}
